#include "main.h"
/**
* _abs - gives absolute value
* @n: The number to be checked
*
* Return: 1 if +ve, 0 if 0, -1 if -ve
*/
int _abs(int n)
{
if (n > 1)
return (1);
else if (n < 1)
return (-1);
else
return (0);
}
