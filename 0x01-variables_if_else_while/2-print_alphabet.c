#include <stdio.h>
#include <ctype.h>
/**
* main - Entry point
* Description: prints last integer
* Using different headers
* Return: Always success 0
*/
int main(void)
{
int c;
for (c = 'a'; c <= 'z'; c++)
putchar(c);
return (0);
}
