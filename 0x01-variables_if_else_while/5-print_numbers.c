#include <stdio.h>
/**
* main - Entry point
* Description: prints digits
* Using different headers
* Return: Always success 0
*/
int main(void)
{
int c;
for (c = 0; c < 10; c++)
printf("%d", c);
putchar('\n');
return (0);
}
